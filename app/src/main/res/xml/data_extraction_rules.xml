<?xml version="1.0" encoding="utf-8"?>
<!--
  Ronda 16: antes este archivo era la plantilla vacia con un TODO. Con
  <cloud-backup> sin reglas, si allowBackup se reactivara se subiria TODO
  (PIN hash, Carpeta Segura cifrada con una clave del Keystore que no viaja
  entre dispositivos, base de datos, documentos). Se excluye explicitamente
  todo, tanto para respaldo en la nube como para transferencia entre
  dispositivos. Aplica a API 31+.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
