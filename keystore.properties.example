# Copia este archivo a keystore.properties (ya está en .gitignore) y
# completa los valores reales. NUNCA subas keystore.properties ni el
# archivo .jks/.keystore al repositorio.
storeFile=keystore/docsmart-release.jks
storePassword=
keyAlias=
keyPassword=
